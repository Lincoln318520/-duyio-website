{
  "private": true,
  "scripts": {
    "build": "node build/build.mjs",
    "images:mexico": "node build/optimize-mexico-images.mjs",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "sharp": "^0.35.2",
    "wrangler": "^4.125.0"
  }
}
